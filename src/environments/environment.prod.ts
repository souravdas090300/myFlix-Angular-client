// Production environment configuration
export const environment = {
  production: true,
  // Use the deployed Heroku API directly in production
  // This is what the tutor expects for the delivered application
  apiUrl: 'https://movieflix-api-p56c.onrender.com/'
};
